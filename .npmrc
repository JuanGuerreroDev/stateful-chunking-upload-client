# Política de pnpm. SE VERSIONA: nunca contiene tokens de auth (van en ~/.npmrc o NPM_TOKEN).

access=public
@juanoecr:registry=https://registry.npmjs.org/

save-exact=true
engine-strict=true
fund=false
